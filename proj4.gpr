library project Proj4 is
   for Languages use ("Ada");
   for Library_Name use "aproj4";
   for Source_Dirs use ("src/**");
   for Object_Dir use ".obj/" & project'Name;
   for Library_Dir use "lib";
   for Library_Kind use "static";
end Proj4;
